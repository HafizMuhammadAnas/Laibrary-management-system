<%= form_with(model: borrow_history) do |form| %>
  <% if borrow_history.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(borrow_history.errors.count, "error") %> prohibited this borrow_history from being saved:</h2>

      <ul>
        <% borrow_history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
